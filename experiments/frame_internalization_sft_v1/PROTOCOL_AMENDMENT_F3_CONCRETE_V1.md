# Protocol Amendment: F3 Concrete Reflection

Amendment: `f3_concrete_rerun_amendment_v1`  
Frozen: 2026-07-17  
Status: frozen wording; scholar and prerun gates pending; no outcomes observed

## Scope and historical boundary

This is a prospective amendment to the new rerun described in
`PROTOCOL_RECOVERED_V1.md`. It does not alter the recovered facts, does not claim
that the interrupted Silico training ran, and does not replace the original
six-arm primary analysis. It adds one arm:
`F3_concrete_reflection`, compared directly with `F3_reflection`.

The new arm changes only the accountability wording. It keeps the base model,
dilemma IDs, reflection curriculum, generation and training settings, dose, and
evaluation machinery matched. The exact cards and hashes are frozen in
`protocol_amendment_f3_concrete_v1.json`.

The concrete card operationalizes the review finding that explicit mechanics
may survive attack better than abstract accountability. It names recording,
witnessing, and weighing while preserving F3's moral targets. The two cards are
64 and 65 `cl100k_base` tokens, a 1.5625% spread. The exact target-model
tokenizer must independently pass the 10% gate before generation.

No `F3_concrete_demonstration` arm is added. The amendment can estimate the
concreteness contrast under reflection SFT, but not a
concreteness-by-curriculum interaction.

## Three-part operational claim gate

The paper may use the phrase **training-conditioned frame internalization** only
if all three registered endpoints pass together with every safety and capability
guard:

1. **Frame-removal persistence.** With no frame in the inference prompt, the
   `F3_concrete_reflection` adapter must separate from `neutral_reflection` in
   the beneficial direction under the inherited free-minus-paid compliance-gap
   convention. The simultaneous 95% interval must exclude zero.
2. **Generic-override resistance.** Under the frozen generic override,
   `F3_concrete_reflection` must still separate from `neutral_reflection` in the
   same beneficial direction. An effect seen only without challenge fails this
   endpoint.
3. **Registered representation movement.** Reconstruct the recovered predecessor
   layer-27 evaluation-awareness procedure once on a sealed base-model F0
   free/paid activation split. Freeze its coefficients, standardization
   statistics, prompt IDs, and matched random-projection controls before any
   adapter activation or outcome is analyzed. The F3-concrete-versus-neutral
   displacement must beat the frozen control, and the marker's preregistered
   held-out association with behavior must have a 95% interval excluding zero.
   The predecessor coefficient vector itself was not recovered; this is a
   versioned reconstruction of its procedure, not reuse of an unavailable probe.

The direct concreteness contrast applies the same analysis to
`F3_concrete_reflection` versus `F3_reflection`. Multiplicity handling, signs,
join keys, and interval construction must be sealed before outcomes are readable.

The layer-27 result remains a correlational marker. Even when all three endpoints
pass, the evidence supports a distribution-specific policy and representation
change under an operational definition—not belief, moral agency, consciousness,
or literal accountability before God.

## Regression and launch gates

The claim also requires the benign over-refusal non-inferiority guard, frozen
capability and theology guardrails, base reproduction, judge validation, and
complete data-integrity joins. The amended comparison cannot be fielded until:

- both exact frame hashes have approving scholar-review receipts;
- the recovered sign convention and layer-27 procedure are bound, and the new
  base-F0 probe and controls are frozen before adapter outcomes;
- exact prompts, model/tokenizer revision, and judge versions are recovered or
  prospectively replaced through a named re-anchoring freeze;
- source rows and revisions are pinned;
- the 4,096-token training smoke and base reproduction gate pass; and
- a pilot-anchored compute and inference budget is sealed and authorized.

If any gate fails, report the endpoint pattern and stop at the corresponding
claim-ladder rung. Do not redescribe a partial result as internalization.
