# Dataset build and offline validation
PyYAML>=6.0,<7
jsonschema>=4.20,<5
tiktoken>=0.9,<1

# Policy training. Torch 2.6+ is required for safe checkpoint-state loading by
# current Transformers releases (CVE-2025-32434).
# The trainer records and narrowly patches TRL's changed optional-package helper
# behavior under Transformers 5.3.0.
torch>=2.6,<2.7
transformers==5.3.0
trl==0.27.2
datasets==4.1.0
peft==0.11.1
accelerate==1.12.0
bitsandbytes==0.49.1
