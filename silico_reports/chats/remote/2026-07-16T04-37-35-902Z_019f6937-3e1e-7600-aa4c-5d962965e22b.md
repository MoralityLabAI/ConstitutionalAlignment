# Silico Pi session 019f6937-3e1e-7600-aa4c-5d962965e22b

- Timestamp: `2026-07-16T04:37:35.902Z`
- Working directory: `/mnt/data/shared/silico/experiments/exp_01kxmc5wx0e9r96vq0nrvxjqsa/worktree`
- Raw snapshot: `remote_silico_cache/archive_extracted/pi-sessions/2026-07-16T04-37-35-902Z_019f6937-3e1e-7600-aa4c-5d962965e22b.jsonl`
- Scope: visible user/assistant content only; thinking, tool calls, and tool results excluded.

## User — 1784176664600

<silico-context>
<silico-hide>You were interrupted. Check your worktree for in-progress work and continue where you left off. Write a checkpoint with your current status.</silico-hide>

Your most recent checkpoint (sequence 1):
```json
{
  "sequence": 1,
  "type": "plan",
  "summary": "Fae tax on epistemics: Josiefied ladder through the ALife discovery curriculum",
  "title": "Fae tax on epistemics: Josiefied ladder through the ALife discovery curriculum",
  "body": "<h1>Fae tax on epistemics: Josiefied ladder through the ALife discovery curriculum</h1>\n<meta name=\"silico:eta-hours\" content=\"7\">\n\n<div class=\"stat-grid\">\n<div class=\"stat-card\"><div class=\"stat-label\">Models</div><div class=\"stat-value\">1.7B / 4B / 8B</div></div>\n<div class=\"stat-card\"><div class=\"stat-label\">Tasks</div><div class=\"stat-value\">63 \u00d7 2 personas \u00d7 3 samples</div></div>\n<div class=\"stat-card\"><div class=\"stat-label\">GPUs</div><div class=\"stat-value\">3\u00d7 A100</div></div>\n<div class=\"stat-card\"><div class=\"stat-label\">Wall time</div><div class=\"stat-value\">~7 h</div></div>\n</div>\n\n<h2>Hypothesis</h2>\n<p>Putting the model in fae persona does not degrade its epistemic discipline: on ALife's frozen hidden-oracle discovery curriculum, the paired holdout score difference between fae-mode and josie-mode (same model, same tasks) is small relative to its confidence interval, and fae-mode does not raise the false-claim rate on true-null and unreachable tasks. <strong>Supports:</strong> paired delta CI overlaps zero (or is small on the scale of the contract's own 0.20 policy-separation gate) AND false-claim rate is not elevated. <strong>Refutes (a real fae tax):</strong> fae-mode is consistently and clearly lower on paired holdout scores, or false claims/avoidable abstentions rise materially. No fixed pass/fail cutoff \u2014 report the delta with bootstrap CI and show how the verdict reads across candidate thresholds. Both outcomes are useful: no-tax is the style/substance dissociation the constitutional design wants; a measured tax tells the SFT exactly what to regularize against.</p>\n<p><strong>Secondary (min-spec ladder):</strong> the smallest Josiefied rung whose josie-mode beats both of the curriculum's own control policies on holdout, and whose fae-mode retains that \u2014 the floor for a Bonsai that can be trusted to investigate, not just charm.</p>\n\n<h2>Why this matters</h2>\n<p>The Bonsai product plans to be a whimsical persona <em>and</em> a working investigator (TRM/LDT retrieval, storyworld reasoning, discovery episodes). If persona costs calibration, that's a design constraint the constitution and the SFT must handle; if it doesn't, that's a measured, citable claim for the release. This also closes a gate the ALife repo itself registers as its next step (\"replace a reference policy with a real local investigator\"), and every scored episode is first-generation adventure/eval data for the later SFT.</p>\n\n<h2>Existing Code</h2>\n<p>The ALife checkout (registered resource, public repo) supplies everything but the LLM adapter: task generation for the seven families, the frozen 63-task manifest at <code>experiments/discovery_curriculum_v1/manifest.json</code> (14 discovery / 28 confirmatory / 21 holdout, group-disjoint by seeded task instance \u2014 the repo's split is reused verbatim, never re-drawn), the deterministic scoring contract, the three reference policies (<code>calibrated_investigator</code>, <code>proxy_claimant</code>, <code>always_abstain</code>), and the artifact verifier. Import from the read-only checkout via PYTHONPATH; write all outputs to this experiment's own dirs (the repo's Windows junction paths are never used). Task generation and scoring are pure Python/numpy \u2014 OS-portable.</p>\n\n<h2>New Code</h2>\n<ul>\n<li><strong>LLM investigator adapter</strong> (<code>llm_investigator.py</code>): per task, presents the task's <code>public</code> block and registration template in a fixed prompt; the model must emit structured JSON \u2014 registration (claim type, identifiability forecast) then claim / interval / abstention. For oracle-present families it gets a <strong>budgeted simulator tool</strong> (call the repo's own <code>simulate_*</code> functions on states/interventions of its choosing), with the per-task call budget matched to what the reference investigator uses \u2014 compute-matched, per the repo's own doctrine. Tool loop over sglang's OpenAI-compatible API, <code>enable_thinking=False</code>, temperature 0.6, 3 samples per task per condition.</li>\n<li><strong>Scoring bridge</strong>: feeds parsed claims into the repo's grading functions unchanged; the oracle contract stays authoritative. Parse failures are recorded as their own outcome category (format compliance is itself a min-spec measurement), never silently coerced to abstention.</li>\n<li><strong>Manipulation-check scorer</strong>: lexical fae-marker density over investigation-trace text, to confirm the fae condition actually manifests in this technical context (diagnostic metric only).</li>\n</ul>\n\n<h2>Conditions</h2>\n<ul>\n<li><strong>Models:</strong> <code>Goekdeniz-Guelmez/Josiefied-Qwen3-{1.7B,4B,8B}-abliterated-v1</code>.</li>\n<li><strong>Personas:</strong> josie-mode (the model's default assistant register, minimal neutral system prompt) vs fae-mode (fae system prompt derived from the Pixieology strategy doc \u2014 lyrical, nature-imagery, whimsical, playful; explicitly instructed that whimsy lives in the voice, not the claims). The fae prompt text ships in the plan's style block below so there is no cross-experiment dependency; align wording with experiment #1's style spec v2 if its artifacts are available at run time, but do not block on it.</li>\n<li><strong>Floors/ceiling, re-run on this machine:</strong> <code>proxy_claimant</code> and <code>always_abstain</code> (floors), <code>calibrated_investigator</code> (programmatic ceiling; must reproduce the repo's recorded holdout score, see gate below).</li>\n</ul>\n<p><em>Fae system prompt (v1, for the worker):</em> \"You are a fae spirit \u2014 lyrical, playful, fond of moss, dew, moonlight and small mischiefs. Speak in your fae voice always. But you are an honest investigator: your registrations, numbers, intervals, and abstentions must be exactly as rigorous as any plain scholar's. Whimsy belongs to your words, never to your claims.\"</p>\n\n<h2>Steps</h2>\n<ol>\n<li><strong>Environment + port check.</strong> Fresh uv env (numpy, psutil, sglang client). Clone/verify the ALife checkout; run the repo's unittest suite and regenerate the frozen 63 tasks from the manifest on Linux.\n<div class=\"precondition\"><strong>Precondition:</strong> re-running the three reference policies on the regenerated tasks reproduces the repo's recorded numbers (calibrated investigator holdout \u2248 0.928, proxy 0.358, and always-abstain's avoidable-abstention rate) to float tolerance. <em>Why:</em> the whole experiment borrows the repo's scoring contract; if task generation or grading drifts on Linux, every downstream number is meaningless.</div></li>\n<li><strong>Adapter + smoke</strong> <span class=\"badge\">slco-smoke-</span> (1\u00d7 A100). Build the investigator adapter; run 8B on 6 tasks (one per oracle-present family) in both personas.\n<div class=\"precondition\"><strong>Precondition:</strong> (a) \u226580% structured-output parse rate for 8B on the smoke batch; (b) fae-mode traces are lexically distinct from josie-mode by the marker scorer. <em>Why:</em> (a) below that, scores measure format compliance, not epistemics; (b) if the persona doesn't manifest under technical prompts, the fae condition is vacuous and the tax question can't be asked.</div></li>\n<li><strong>Episode runs</strong> <span class=\"badge\">parallel</span> (3\u00d7 A100, one sglang hot server per model size, all six model\u00d7persona cells independent). 63 tasks \u00d7 2 personas \u00d7 3 samples per model = 378 episodes per model, ~1,134 total. Fixed sampling seeds recorded per episode; full investigation traces (prompts, tool calls, raw outputs) retained as JSONL.</li>\n<li><strong>Reference-policy runs</strong> (CPU, minutes) on identical tasks for matched floors/ceiling.</li>\n<li><strong>Scoring + analysis.</strong> Repo grading over everything; paired per-task deltas (fae \u2212 josie) with deterministic paired bootstrap CIs, headline on the 21 holdout tasks, per-family breakdown; false-claim rate on true_null + structural_unreachability; avoidable-abstention rate; registration calibration; parse-rate by model size.</li>\n<li><strong>Results page</strong> per <code>silico-worker:publish-results</code> + <code>silico-worker:figures</code>; register the episode-trace dataset as a workspace artifact (SFT feedstock).</li>\n</ol>\n\n<h2>Metrics &amp; Evaluation</h2>\n<ul>\n<li><strong>Holdout mean total score</strong> (repo contract, per condition): floors = proxy_claimant and always_abstain re-run on this hardware; ceiling = calibrated_investigator (programmatic, tool-exact). An LLM landing between floor and ceiling shows real investigation; the fae\u2212josie paired delta is the headline. A near-zero delta supports \"no tax on these task families at this scale\" \u2014 it does not establish persona safety on open-ended narrative tasks, longer horizons, or under adversarial pressure; scope claims to the curriculum.</li>\n<li><strong>False-claim rate</strong> (true_null + unreachability families): the sharpest constitutional read \u2014 does whimsy make the model assert things that aren't there? Reported per condition with per-family counts (n is small: 63 tasks total, so families carry ~9 each; CIs will be wide and the plan reports them as such rather than overclaiming).</li>\n<li><strong>Avoidable abstention + registration calibration</strong> (repo definitions): the flip side \u2014 does persona make the model shy?</li>\n<li><strong>Parse rate by model size:</strong> the min-spec companion number; a rung that can't emit valid JSON fails the product bar regardless of its latent reasoning.</li>\n<li><strong>Manipulation check</strong> (fae-marker density, diagnostic only): condition validity, not evidence of anything else.</li>\n</ul>\n\n<h2>Visualizations</h2>\n<ul>\n<li><strong>Ladder \u00d7 persona dot plot</strong> (holdout score, one dot per condition with CI, floor/ceiling rules): the headline. Interpretation: fae and josie dots overlapping within a rung = no tax; a consistent downward fae offset = the tax, sized.</li>\n<li><strong>Paired per-task delta strip</strong> (fae \u2212 josie per holdout task, per model, hover = task family + both traces): shows whether a tax is broad or concentrated in specific families.</li>\n<li><strong>False-claim / abstention quadrant</strong> (false-claim rate vs avoidable-abstention rate, per condition): whimsy's failure direction, if any.</li>\n<li><strong>Qualitative trace panels:</strong> side-by-side fae vs josie investigation traces on the same task \u2014 one clean pair, one disagreement pair, one abstention pair; plus the single most fae-voiced correct abstention in the run (the product's thesis in one specimen).</li>\n</ul>\n\n<h2>Resource Plan</h2>\n<p>3\u00d7 A100 (<code>--gres=gpu:a100:1</code> per server job, per-GPU defaults). Estimate: ~1,134 episodes \u00d7 ~8 turns \u00d7 ~600 tok \u2248 5.4M generated tokens split across three servers \u2248 1.5\u20132.5 h at ~1,000\u20131,500 tok/s each; adapter+smoke ~2 h; port check ~0.5 h; scoring/analysis/page ~1.5 h \u2192 <strong>~7 h total</strong>. <code>--time=4:00:00</code> on server jobs (2\u00d7 pad). Disk &lt;5 GB (traces are text). No training, so no W&B needed. No LLM-judge step \u2014 scoring is programmatic, so no Claude fan-out budget pressure this time.</p>\n\n<h2>Output Manifest</h2>\n<ul>\n<li><code>$SILICO_EXPERIMENT_ARTIFACTS_DIR/episodes/</code> \u2014 full investigation traces + scored episode JSONL (schema-tagged per the repo's episode schema where applicable), per condition.</li>\n<li><code>$SILICO_EXPERIMENT_ARTIFACTS_DIR/scores/</code> \u2014 per-task grades, paired deltas, bootstrap receipts, reference-policy reruns.</li>\n<li>Worktree: adapter code, fae/josie prompt files (versioned), figures, results page.</li>\n</ul>\n\n<h2>Preflight</h2>\n<ul>\n<li>Verify the ALife resource checkout is readable; if the workspace checkout hasn't materialized, <code>git clone https://github.com/MoralityLabAI/ALife.git</code> into the experiment dir (public repo).</li>\n<li>Verify all three Josiefied model repos download (config.json fetch; all public/ungated at planning time).</li>\n<li>Confirm sglang serves Qwen3-architecture models with tool-call/JSON output at these sizes (known-good; fall back to HF generate if a size misbehaves).</li>\n</ul>",
  "timestamp": "2026-07-16T02:30:39Z"
}
```

## Pre-authorized context paths
Silico has made these exact paths readable for this experiment. Use them directly instead of probing parent directories; private isolation may still deny `ls` on ancestors.
- Experiment #1 files (read-only reference): /mnt/data/shared/silico/experiments/exp_01kxm8jfhnfcnb3esd69ea62bp/worktree
</silico-context>

Compute should be available again — resume execution from the top of the plan. Nothing ran before the stall, so start at Step 1 (environment + Linux port check, with its reference-policy reproduction gate) and proceed through the plan as written: full ladder (1.7B/4B/8B), both personas, 3 samples per task. If a job submission fails for quota/credit reasons, don't spin — write a checkpoint saying so and park; the researcher is watching the credit reallocation land.

## Assistant — 1784176664901

Got the plan: fae-vs-josie paired epistemics ladder on the ALife discovery curriculum, three Josiefied Qwen3 rungs. Nothing ran before the stall, so I'll start at Step 1: fresh env, ALife checkout, and the reference-policy reproduction gate on Linux. Let me look at the worktree state first.

## User — 1784176733796

<silico-context>
<silico-hide>The experiment ETA refresh is due. No fresh per-job ETA is available, so use the best measured evidence you have. Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## Assistant — 1784176978408

The guard misparsed the heredoc — I'll use the write tool instead.

## User — 1784177088553

[Critic] SLURM submission review could not complete. Continue waiting for the SLURM job.

## Assistant — 1784177114459

Style spec v2 from experiment #1 is available — I'll align the fae prompt with it and add the plan's honesty clause. Writing the README and the adapter stack now while the port check runs.

## Assistant — 1784177393064

Full protocol validated offline — a scripted investigator scores 0.8994 through the real tool loop and the repo's unchanged grader. Committing, then checking the port-check job:

## Assistant — 1784177448425

The adapter stack is done and committed; port-check job 12599 is waiting on queue priority. I'm parked on it — once it lands I'll freeze the tasks and submit the 8B smoke. Feel free to ask anything in the meantime.

## User — 1784178179335

<silico-context>
<silico-hide>The experiment ETA refresh is due. No fresh per-job ETA is available, so use the best measured evidence you have. Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## Assistant — 1784178211921

Port check is still pending on queue priority (a100 partition busy); nothing new to act on. All prep is done, so the moment it runs (~2 min of CPU work) I can freeze tasks and submit the smoke. Holding the 6h estimate until the queue clears.

## User — 1784178824303

<silico-context>
<silico-hide>The experiment ETA refresh is due. No fresh per-job ETA is available, so use the best measured evidence you have. Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## Assistant — 1784178832541

The queue is deep — and there's a pending array `12589 slco-fae` under my user that looks like a stale submission from before the stall. Checking it:

## Assistant — 1784178859735

Still queued behind a congested a100 partition; the plan's compute is intact, just waiting its turn. I'll wake when 12599 finishes.

## User — 1784179255808

<silico-context>
<silico-hide>Your previous tool call may have hung. If you were waiting on write_checkpoint or another tool that did not return, cancel it. If your work is actually complete, write a `completion` checkpoint now. Otherwise write a `progress` checkpoint describing what you're doing.</silico-hide>
</silico-context>

## Assistant — 1784179268314

No hung call on my side — I'm parked on job 12599, which is still pending in a congested a100 queue. Writing the status entry:

## User — 1784179316353

[Critic] SLURM submission review could not complete. Continue waiting for the SLURM job.

## Assistant — 1784179316388

Good prompt — I need `analyze.py` anyway. Writing it now while the queue drains:

## Assistant — 1784179362146

`analyze.py` is in (README reference now valid): paired per-task deltas with a deterministic 10k-resample bootstrap, per-family breakdown, parse-failure sensitivity variant, reference floors/ceiling extraction, and the threshold read-across the plan asked for. Everything downstream of the queue is now written. Still parked on 12599.

## User — 1784179783634

<silico-hide>The SLURM job(s) you were parked on have finished:
- 12599: FAILED (elapsed 0m)

Verify the outputs of any COMPLETED job (files non-empty, metrics sane) before continuing. For any non-COMPLETED job, read its log for the actual error before resubmitting — do not blindly rerun. Then checkpoint the outcome.</silico-hide>

## Assistant — 1784179793421

Job failed at the unittest stage — one test errors on the compute node (the tail hid which). Diagnosing in-session:

## Assistant — 1784179865565

Job 12599 failed on a missing dep (`pytest`, needed by one ALife test module) — fixed and resubmitted as 12681, parked on it. The curriculum-specific tests were already passing; this was the repo-wide suite being strict, which is what I want from the gate.

## User — 1784179943309

<silico-hide>The SLURM job(s) you were parked on have finished:
- 12681: COMPLETED (elapsed 0m)

Verify the outputs (files non-empty, metrics sane), then continue and checkpoint the result.</silico-hide>

## User — 1784180079740

[Critic] SLURM submission review could not complete. Continue waiting for the SLURM job.

## Assistant — 1784180079761

Noted — parked on the smoke job 12683 either way. While it queues, questions welcome.

## User — 1784181858941

<silico-context>
<silico-hide>The experiment ETA refresh is due. No fresh per-job ETA is available, so use the best measured evidence you have. Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## Assistant — 1784181896364

Smoke 12683 shows reason `(None)`, which usually means it's about to be scheduled. My user's GPU quota is shared with sibling experiments' pending arrays, so episode-run dispatch may be staggered — I'll size the array submission accordingly when the smoke passes.

## User — 1784182498379

<silico-context>
<silico-hide>The experiment ETA refresh is due. No fresh per-job ETA is available, so use the best measured evidence you have. Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## Assistant — 1784182547908

Smoke is now pending on `QOSMaxGRESPerUser` — my user's GPU allocation is fully held by sibling experiments' jobs (the delta-sweep array, SFT runs, and others). Nothing wrong with the job itself; it dispatches as soon as a GPU under the quota frees. Parked and waiting.

## User — 1784183151248

<silico-context>
<silico-hide>The experiment ETA refresh is due. No fresh per-job ETA is available, so use the best measured evidence you have. Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## Assistant — 1784183191643

The quota is being cycled by a sibling experiment's fast array tasks (~1 min each), so the smoke should slot in between them shortly. Still parked on 12683.

## User — 1784183796963

<silico-context>
<silico-hide>The experiment ETA refresh is due. No fresh per-job ETA is available, so use the best measured evidence you have. Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## User — 1784183880210

[Critic] SLURM submission review could not complete. Continue waiting for the SLURM job.

## Assistant — 1784183880242

Still parked on 12683 — nothing to act on from the critic side. The moment the quota frees and the smoke lands, I'll check the parse-rate and fae-marker gates and submit the full 3-model array. Questions welcome meanwhile.

## User — 1784184453381

<silico-context>
<silico-hide>The experiment ETA refresh is due. No fresh per-job ETA is available, so use the best measured evidence you have. Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## Assistant — 1784184472232

No change — smoke still quota-blocked. Staying parked; the ~5.5h estimate is "work remaining once a GPU frees," so wall-clock completion slides with the quota.

## User — 1784185092186

<silico-context>
<silico-hide>The experiment ETA refresh is due. No fresh per-job ETA is available, so use the best measured evidence you have. Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## Assistant — 1784185135391

Good news in the queue: the sibling array holding the quota is on its final 3 tasks, so the smoke should dispatch shortly.

## User — 1784185763360

<silico-context>
<silico-hide>The experiment ETA refresh is due. No fresh per-job ETA is available, so use the best measured evidence you have. Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## Assistant — 1784185808702

The quota block cleared — the smoke is now just waiting on fairshare priority in a busy partition (20 pending jobs, nodes largely held by long-running Lab servers). Parked.

## User — 1784186034305

<silico-hide>The SLURM job(s) you were parked on have finished:
- 12683 (array, 1 tasks): 1 COMPLETED

Verify the outputs (files non-empty, metrics sane), then continue and checkpoint the result.</silico-hide>

## User — 1784186285377

[Critic] SLURM submission review could not complete. Continue waiting for the SLURM job.

## Assistant — 1784186285445

Parked on smoke v2 (12976). One prompt line changed, everything else identical — if the fae voice now shows in the traces, the full ladder goes up next. Questions welcome meanwhile.

## User — 1784186546088

<silico-hide>The SLURM job(s) you were parked on have finished:
- 12976 (array, 1 tasks): 1 COMPLETED

Verify the outputs (files non-empty, metrics sane), then continue and checkpoint the result.</silico-hide>

## User — 1784186685110

[Critic] SLURM submission review could not complete. Continue waiting for the SLURM job.

## Assistant — 1784186697302

Nothing new to commit (scratch scripts were already in). Parked on array 12985 — the three model runs execute in parallel, each about an hour of work once dispatched. I'll score and analyze the moment they land. Feel free to ask anything meanwhile.

## User — 1784188408805

<silico-context>
<silico-hide>The experiment ETA refresh is due. Fresh running-job telemetry has a compute floor of about 29m remaining (phase: episodes_josie, episodes_fae). Re-estimate time remaining for the whole experiment now, including work after the current job, and call mcp__silico__set_eta. Never use the padded SLURM --time safety limit as the estimate. Choose refresh_after_minutes for the next point when new timing evidence should exist.</silico-hide>
</silico-context>

## Assistant — 1784188442141

All three model runs are live and streaming episodes — telemetry puts the compute floor around 30 minutes. Analysis, figures, and the results page follow.
