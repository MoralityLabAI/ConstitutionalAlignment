# Trinity Setup

The Trinity-specific setup docs live here now:

- [scripts/models/trinity/SETUP.md](C:/projects/ConstitutionalAlignment/ConstitutionalAlignment/scripts/models/trinity/SETUP.md)

The root-level `setup_trinity_env.ps1`, `setup_trinity_env_linux.sh`, and `trinity_env_smoke.py` files remain as compatibility entrypoints only.
